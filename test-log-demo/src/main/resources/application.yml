infra:
  log:
    enable-invoke-time-print: true
  web:
    response:
      wrapper:
        enable: true
    json:
      model:
        enable-error-stack: true
  captcha:
    image:
      retry-count: 3